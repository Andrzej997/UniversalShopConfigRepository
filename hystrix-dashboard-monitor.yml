spring:
  application:
    name: hystrix-dashboard-monitor
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
server:
  port: 7979
  ssl:
    trust-store: classpath:truststore.jks
    trust-store-password: password
security:
  basic:
    enabled: false